# ============================================================
# NN 推理依赖（「常规使用」含 NN 融合打分，也需要安装本文件）
# 安装: pip install -r requirements-nn.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# 说明: 在 requirements.txt 基础上追加安装。
#       - 常规使用（程序 + NN 融合打分）→ requirements.txt + 本文件
#       - 微调训练 → 再额外装 requirements-nn-train.txt
# ============================================================

# ---- 推理运行时（NN 融合打分必需）----
torch>=2.1                 # PyTorch 运行时（CPU 版即可，pip 默认装 CPU 版；
                           #   重的是 CUDA 版，推理用不到，勿装）
transformers>=4.40         # BGE 微调模型加载 + tokenizer
safetensors>=0.4           # 权重格式（fine_tuned/model.safetensors）

# ---- 模型下载（境内网络）----
modelscope>=1.14           # 下载 hajishou 微调模型；训练时也用它下 BGE 基座

