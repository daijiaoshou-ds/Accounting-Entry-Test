# ============================================================
# NN 训练依赖（仅「微调/训练模型」时需要，普通用户无需安装）
# 安装: pip install -r requirements-nn-train.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# 前提: 已装 requirements.txt + requirements-nn.txt。
#       GPU 训练另需按 https://pytorch.org 装 CUDA 版 torch。
# ============================================================

# ---- 训练增强 ----
accelerate>=0.30           # 分布式/混合精度训练加速
peft>=0.12                 # LoRA 微调策略

